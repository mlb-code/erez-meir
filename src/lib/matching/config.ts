/**
 * קבועים ניתנים לכוונון של מנוע ההתאמות.
 * כל שינוי בערכים כאן משנה את הציון שמוצג למשתמש — ולא את הלוגיקה של מי מתאים למי.
 */
export const MATCHING_CONFIG = {
  /** אורך מעגל מקסימלי. 2 = התאמה ישירה, 3–5 = שרשרת. */
  MAX_CHAIN_LENGTH: 5,

  /** נקודת הפתיחה של כל התאמה שעברה את כל תנאי הסף. */
  BASE_SCORE: 50,

  /** בונוס להתאמה ישירה (מעגל של 2) — פשוטה יותר לביצוע משרשרת. */
  DIRECT_BONUS: 12,

  /** קנס על כל משתתף נוסף מעבר לשניים. שרשרת ארוכה = יותר נקודות כשל. */
  CHAIN_LENGTH_PENALTY: 6,

  /** משקל רכיב "קרבת השווי" — כמה מזומן צריך לעבור מיד ליד. */
  VALUE_PROXIMITY_WEIGHT: 25,

  /**
   * פער מזומן, כשיעור משווי הדירה, שמאפס את רכיב קרבת השווי.
   * 0.15 = פער של 15% ומעלה נחשב "רחוק".
   */
  VALUE_GAP_ZERO_RATIO: 0.15,

  /** משקל רכיב "עודף על הקריטריונים" — הדירה שאקבל טובה מהמינימום שביקשתי. */
  SURPLUS_WEIGHT: 13,

  /** עודף חדרים שמעליו הרכיב מלא (חצי חדר ומעלה נחשב, חדר וחצי = מקסימום). */
  ROOMS_SURPLUS_CAP: 1.5,

  /** עודף שטח במ״ר שמעליו הרכיב מלא. */
  SQM_SURPLUS_CAP: 30,
} as const;
